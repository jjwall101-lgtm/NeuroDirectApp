# Firebase setup - NeuroDirect v12

Firebase config is already inserted.

1. Enable Authentication -> Email/Password.
2. Create Firestore Database.
3. Paste and publish `firestore.rules`.
4. Open NeuroDirect -> Settings -> create profile -> Save profile to cloud.

This version syncs profile summary only. Full task/check-in sync can be added next.
